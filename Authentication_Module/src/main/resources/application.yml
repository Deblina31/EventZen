server:
  port: 8080

spring:
  application:
    name: eventZen

  datasource:
    url: jdbc:mysql://localhost:3306/eventmanagerdb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  main:
    banner-mode: off

#logging:
#  level:
#    org.springframework.security: DEBUG
#    org.hibernate.SQL: DEBUG
#    org.hibernate.orm.jdbc.bind: TRACE
#    root: WARN

#jwt:
#  secret: mySuperSecretKeyForJwtAuthenticationMySuperSecretKey123
#  expiration: 36000000
